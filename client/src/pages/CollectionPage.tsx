import { useEffect, useState } from "react";
import { ArrowLeft, ArrowUpRight, LockKeyhole } from "lucide-react";
import { Link } from "wouter";
import type { ReactNode } from "react";
import { supabase } from "@/lib/supabase";

type CollectionPageProps = { family: "Neocaridina" | "Caridina"; image: string; number: string; title: ReactNode; intro: string; details: string };
type CatalogLine = { id: string; name: string; description: string; availability: "available" | "limited" | "accepting_requests" | "out_of_stock" | "coming_soon"; price: number; quantity: number; minimum_order: number; image_url: string | null };
const familyLines: Record<Lowercase<CollectionPageProps["family"]>, string[]> = { neocaridina: ["Skittles", "Fire Red", "Blue Dream", "Ocean Blue", "Bloody Mary", "Schoko", "Tangerine", "Golden Back", "Rili", "Culls"], caridina: ["Blue Bolt", "King Kong", "Shadow Panda", "Boa", "Galaxy", "Galaxy Tiger", "Wine Red", "Snowfall", "Stardust", "Spectre Tiger"] };
const availabilityLabels: Record<CatalogLine["availability"], string> = { available: "Available", limited: "Limited availability", accepting_requests: "Accepting requests", out_of_stock: "Out of stock", coming_soon: "Coming soon" };
function fallbackLines(family: CollectionPageProps["family"]): CatalogLine[] { return familyLines[family.toLowerCase() as Lowercase<CollectionPageProps["family"]>].map((name, index) => ({ id: `fallback-${index}`, name, description: "Catalog description to be finalized.", availability: "out_of_stock", price: 0, quantity: 0, minimum_order: 0, image_url: null })); }

export default function CollectionPage({ family, image, number, title, intro, details }: CollectionPageProps) {
  const familyKey = family.toLowerCase() as "neocaridina" | "caridina"; const [lines, setLines] = useState<CatalogLine[]>(fallbackLines(family)); const [catalogLoading, setCatalogLoading] = useState(true);
  useEffect(() => { let mounted = true; setCatalogLoading(true); supabase.from("inventory_items").select("id, name, description, availability, price, quantity, minimum_order, image_url").eq("family", familyKey).order("sort_order", { ascending: true }).then(({ data }) => { if (mounted) { setLines(data && data.length > 0 ? data as CatalogLine[] : fallbackLines(family)); setCatalogLoading(false); } }); return () => { mounted = false; }; }, [family, familyKey]);
  return <div className="detail-page"><section className="detail-hero page-width"><Link href="/" className="back-link"><ArrowLeft size={15} /> Back to home</Link><div className="detail-hero__grid"><div className="detail-hero__copy"><div className="section-label"><span>{number}</span> Collection study / {family}</div><h1>{title}</h1><p>{intro}</p><span className="status-pill"><span /> Catalog developing / requests welcome</span></div><figure className="detail-hero__image"><img src={image} alt={`${family} shrimp in a planted aquarium`} /><figcaption>Visual study / {family.toLowerCase()} in planted water</figcaption></figure></div></section><section className="detail-body section-pad"><div className="page-width detail-body__grid"><div className="section-label"><span>—</span> A note on the collection</div><div><h2>{details}</h2><p>The initial {family} lines are now listed below as a working catalog. Every entry is currently marked out of stock while permits, sourcing, and business costs are finalized.<br /><br />Placeholder pricing, quantities, shipping policies, arrival policies, and payment information are intentionally marked for later revision.</p><Link href="/about#contact" className="text-link">Talk to Ebi Tsū <ArrowUpRight size={15} /></Link></div></div></section><section className="catalog-section section-pad"><div className="page-width"><div className="section-heading"><div><span className="section-label"><span>03</span> {family} catalog</span><h2 className="catalog-heading">Browse the<br /><em>working list.</em></h2></div><span className="section-heading__note">{lines.length} lines / managed availability</span></div>{catalogLoading ? <p className="catalog-loading">Loading catalog...</p> : <div className="catalog-grid">{lines.map((line, index) => <article className="catalog-card" key={line.id}><div className="catalog-card__image"><img src={line.image_url || image} alt={`${line.name} shrimp placeholder`} /><span className={`catalog-card__availability catalog-card__availability--${line.availability}`}>{availabilityLabels[line.availability]}</span></div><div className="catalog-card__content"><div><span className="catalog-card__number">{family} / {String(index + 1).padStart(2, "0")}</span><h3>{line.name}</h3><p>{line.description}</p></div><div className="catalog-card__facts"><span><small>Price</small>{line.price === 0 ? "$0 placeholder" : `$${line.price.toFixed(2)}`}</span><span><small>Quantity</small>{line.quantity}</span><span><small>Minimum</small>{line.minimum_order}</span></div><div className="catalog-card__footer">{line.availability === "out_of_stock" ? <span className="catalog-card__placeholder"><LockKeyhole size={13} /> Purchase unavailable</span> : <span className="catalog-card__placeholder">Request or purchase details</span>}<Link href="/livestock-request" className="text-link">Request this line <ArrowUpRight size={14} /></Link></div></div></article>)}</div>}<div className="catalog-note"><span className="section-label"><span>—</span> Before availability</span><p>These entries are public placeholders for the future catalog. No purchases can be placed while a line is out of stock. Prices, quantities, shipping rules, live-arrival policy, and payment method will be updated before any listing becomes available.</p></div></div></section></div>;
}
